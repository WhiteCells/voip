# 软电话服务端依赖部署说明

## 1. 概述
软电话服务端依赖多个第三方库，在不同平台上需要不同的编译步骤。  
本文将分别说明 **Windows (Visual Studio 2019)** 与 **Linux (GCC)** 下的构建方法。

---

## 2. 依赖列表

### BOOST
```bash
git clone https://github.com/boostorg/boost.git
# 或使用 SSH
git clone git@github.com:boostorg/boost.git
```

### JRTPLIB
```bash
git clone https://github.com/j0r1/JRTPLIB.git
git clone git@github.com:j0r1/JRTPLIB.git
```

### JThread
```bash
git clone https://github.com/j0r1/JThread.git
git clone git@github.com:j0r1/JThread.git
```

### JsonCpp
```bash
git clone https://github.com/open-source-parsers/jsoncpp.git
git clone git@github.com:open-source-parsers/jsoncpp.git
```

### Pjproject
```bash
git clone https://github.com/pjsip/pjproject.git
git clone git@github.com:pjsip/pjproject.git
```

### Spdlog
```bash
git clone https://github.com/gabime/spdlog.git
git clone git@github.com:gabime/spdlog.git
```

---

## 3. Windows 平台编译 (Visual Studio 2019, MSVC)

### 3.1 Boost 编译（手动编译）
```powershell
cd boost
bootstrap.bat
b2 --build-dir=build --build-type=complete toolset=msvc variant=release,debug threading=multi address-model=64 install --prefix="C:\Boost"
```
> 编译完成后，在 Visual Studio 项目中配置 **Include 目录** 和 **Library 目录** 指向 `C:\Boost\include` 与 `C:\Boost\lib`。

---

### 3.2 Pjproject 编译（手动在 VS2019 中打开）
1. 打开 `pjproject` 目录中的 Visual Studio 解决方案文件（如 `pjproject-vs14.sln`）。
2. 在 VS2019 中选择 `x64` 和 `Release` 模式进行编译。
3. 将生成的 `.lib` 和 `.dll` 文件放入项目的依赖目录，并配置 Include 路径。

---

### 3.3 其他依赖（CMake 编译）
在项目根目录执行：
```powershell
cmake -S . -B build -G "Visual Studio 16 2019" -A x64 -T host=x64
cmake --build build --config Release
```
> 注意：Visual Studio 2022 对应生成器为 `"Visual Studio 17 2022"`。

---

## 4. Linux 平台编译 (GCC)

### 4.1 Boost 编译（手动编译）
```bash
cd boost
./bootstrap.sh
./b2 --build-dir=build --build-type=complete toolset=gcc variant=release,debug threading=multi address-model=64 install --prefix=/usr/local/boost
```
> 可能需要 `sudo make install` 权限，并将 `/usr/local/boost/lib` 添加到 `LD_LIBRARY_PATH`。

---

### 4.2 其他依赖（CMake 编译）
```bash
cmake -S . -B build
cmake --build build --config Release
```

---

## 5. 注意事项
- Windows 下 Pjproject 必须手动在 Visual Studio 中编译，不能直接用 CMake。
- Boost 在两个平台都建议手动编译，并指定安装路径，方便统一管理。
- 其余依赖（JRTPLIB、JThread、JsonCpp、Spdlog）均可直接用 CMake 构建。
- 保证编译器与依赖的架构一致（x64 对应 x64）。
- 若使用动态库，记得在运行时设置 `PATH` (Windows) 或 `LD_LIBRARY_PATH` (Linux) 指向库文件所在目录。
